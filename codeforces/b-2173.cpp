#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <algorithm>
#include <set>
#include <random>
#include <map>

using namespace std;

int solve(vector<int> a, vector<int> b){
    
    return s;
}

int main(){
    // ifstream cin("in.txt");
    int t;
    cin >> t;
    vector<int> a;
    while (t--) {
        int n;
        cin>>n;
        int m = n;
        vector<int> a;
        while(n--){
            int elt;
            cin >> elt;
            a.push_back(elt);
        }
        vector<int> b;
        while(m--){
            int elt;
            cin >> elt;
            b.push_back(elt);
        }
        cout<<solve(a, b)<<endl;
    }
}
